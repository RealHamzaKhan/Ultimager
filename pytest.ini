[pytest]
asyncio_mode = auto
timeout = 30
testpaths = tests
addopts = --cov=app --cov-report=term-missing --cov-fail-under=35 --ignore=test_complete_system.py -x
norecursedirs =
    .git
    .pytest_cache
    venv
    uploads
    tests/test_comprehensive_dataset
    test_datasets
